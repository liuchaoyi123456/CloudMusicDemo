<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- 状态栏占位 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="24dp"
        android:background="#FF2C55" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#FF2C55"
        android:gravity="center_vertical"
        android:paddingLeft="16dp"
        android:paddingRight="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="网易云音乐"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Deleted:<ImageView -->
        <!-- Deleted:    android:id="@+id/ivVoiceAssistant" -->
        <!-- Deleted:    android:layout_width="40dp" -->
        <!-- Deleted:    android:layout_height="40dp" -->
        <!-- Deleted:    android:src="@android:drawable/ic_btn_speak_now" -->
        <!-- Deleted:    android:contentDescription="语音助手" -->
        <!-- Deleted:    android:padding="6dp" -->
        <!-- Deleted:    android:clickable="true" -->
        <!-- Deleted:    android:focusable="true" -->
        <!-- Deleted:    android:background="?attr/selectableItemBackgroundBorderless" -->
        <!-- Deleted:    android:tint="#FFFFFF" /> -->
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#F5F5F5">
    </androidx.recyclerview.widget.RecyclerView>

</LinearLayout>